TODO:

- don't delete everything on get (DONE)
- create unique folder name per session based on id (maybe use tempfile see below) (NOT NEEDED)
- clean up on download based on session id (NOT NEEDED)
- Clean up naming conventions (in progress) 
- use bootstrap css to improve UX (in progress)
- test deployment on cpanel (Deployed original code in cpanel - yet to deploy the latest changes)
- document photo taking process requirements (in progress)



@app.route('/')
def index():
    session.dir = tempfile.TemporaryDirectory(dir='static').name
    return str(session.dir)